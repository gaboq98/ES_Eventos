
<div class="d-flex justify-content-center align-items-center">
    <div class="container">
        <div class="row align-items-end">
            <div class="col-12">
                <h1 class="display-4 text-center pb-5">Bienvenido</h1>
                <div class="p-2 text-center">
                    @using (Html.BeginForm("Index", "Paquetes"))
                    {
                        <button type="submit" class="btn btn-primary mr-1" style="width:300px;">Ver paquetes</button>
                    }
                </div>
                <div class="p-2 text-center">
                    @using (Html.BeginForm("OpenReservaciones", "Home"))
                    {
                        <button type="submit" class="btn btn-primary mr-1 " style="width:300px;">Reservaciones</button>
                    }
                </div>
                <div class="p-2 text-center">
                    @using (Html.BeginForm("Historial", "Reservaciones"))
                    {
                        <button type="submit" class="btn btn-primary mr-1" style="width:300px;">Historial de reservaciones</button>
                    }
                </div>
            </div>
        </div>
    </div>
</div>
